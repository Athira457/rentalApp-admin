/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:  ['127.0.0.1', 'localhost'], // Add both domains for local development
      },
};

export default nextConfig;
